<h1>Scores</h1>

<table class="table table-striped">
  <tr>
    <th>Application</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @scores.each do |score| %>
<% name = (score.application.user.name unless application.blank? or score.application.user.blank) || "" %>
  <tr>
    <td><%= link_to name application_path score.application_id %></td>
    <td><%= link_to 'Show', score %></td>
    <td><%= link_to 'Edit', edit_score_path(score) %></td>
    <td><%= link_to 'Destroy', score, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
